# Copy to .env.local to override the checked-in .env.development/.env.production
# defaults for your own machine (e.g. a different local port or a LAN IP for
# testing from another device). .env.local is gitignored; this file is the template.
VITE_API_BASE_URL=http://localhost:5227/api/v1
