# Loaded automatically by `npm run dev` (Vite's default mode is "development").
# Single source of truth for the admin panel's local-dev backend URL — see
# ../backend/src/TrafficJam.Api/appsettings.Development.json's Api:PublicBaseUrl,
# which must match this.
VITE_API_BASE_URL=http://localhost:5227/api/v1
